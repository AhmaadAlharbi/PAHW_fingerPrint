@{
ViewData["Title"] = "تسجيل الدخول";
var errorMsg = TempData["ErrorMsg"] as string;
var successMsg = TempData["SuccessMsg"] as string;
}



<div class="login-page">
<!-- Login Container -->
<div class="login-container">
    <div class="login-card">
        <!-- Decorative Circles -->
        @* <div class="decorative-circle circle-1"></div> *@
        @* <div class="decorative-circle circle-2"></div> *@

        <!-- Login Header -->
        <div class="login-header">
            <div>
                <img src="https://www.pahw.gov.kw/images/PAHW-Logo.png" alt="شعار المؤسسة" />
            </div>
            <h1 class="login-title">تسجيل الدخول</h1>
            <p class="login-subtitle">مرحباً بك في نظام إدارة البصمات</p>
        </div>

        <!-- Error Alert -->
        @if (!string.IsNullOrWhiteSpace(errorMsg))
        {
        <div class="alert-error">
            <div class="alert-icon-error">❌</div>
            <div class="alert-message">@errorMsg</div>
        </div>
        }

        <!-- Success Alert -->
        @if (!string.IsNullOrWhiteSpace(successMsg))
        {
        <div class="alert-success">
            <div class="alert-icon-success">✅</div>
            <div class="alert-message">@successMsg</div>
        </div>
        }

        <!-- Login Form -->
        <form method="post" asp-controller="Home" asp-action="Login">
            @Html.AntiForgeryToken()

            <!-- Username Field -->
            <div class="form-group">
                <label class="form-label">
                    <span>👤</span>
                    <span>الرقم الوظيفي</span>
                </label>
                <div class="input-wrapper">
                    <input type="text"
                           name="empId"
                           class="form-input"
                           placeholder="أدخل الرقم الوظيفي"
                           required
                           autofocus />
                    <span class="input-icon">👤</span>
                </div>
            </div>

            <!-- Password Field -->
            <div class="form-group">
                <label class="form-label">
                    <span>🔒</span>
                    <span>كلمة المرور</span>
                </label>
                <div class="input-wrapper">
                    <input type="password"
                           name="password"
                           class="form-input"
                           placeholder="أدخل كلمة المرور"
                           id="passwordInput"
                           required />
                    <span class="input-icon">🔒</span>
                    <button type="button" class="password-toggle" id="togglePasswordBtn" aria-label="إظهار أو إخفاء كلمة المرور">
                        <i class="fa-solid fa-eye"></i>
                    </button>
                </div>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn-login">
            
                <span>دخول</span>
                <span>🔓</span>
            </button>
        </form>

        <!-- Footer -->
        <div class="login-footer">
            <p class="footer-text">
            

             <span style="display: block;">   جميع الحقوق محفوظة © 2026</span>
                <span class="footer-brand">المؤسسة العامة للرعاية السكنية</span>
            </p>
            <p>v0.1.0-beta</p>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const input = document.getElementById('passwordInput');
            const btn = document.getElementById('togglePasswordBtn');
            if (!input || !btn) return;

            btn.addEventListener('click', function () {
                const isPassword = input.type === 'password';
                input.type = isPassword ? 'text' : 'password';

                const icon = btn.querySelector('i');
                if (icon) {
                    icon.classList.toggle('fa-eye', !isPassword);
                    icon.classList.toggle('fa-eye-slash', isPassword);
                }
            });
        });
    </script>
}
</div>
