@model ErrorViewModel
@{
    ViewData["Title"] = "خطأ";
}

<h1 class="text-danger">حدث خطأ.</h1>
<h2 class="text-danger">حدث خطأ أثناء معالجة الطلب.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>رقم الطلب:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>وضع التطوير</h3>
<p>
    عند التشغيل في <strong>بيئة التطوير</strong> ستظهر تفاصيل أكثر عن الخطأ.
</p>
<p>
    <strong>لا يُنصح بتفعيل بيئة التطوير في الإنتاج.</strong>
    لأن ذلك قد يُظهر معلومات حساسة للمستخدمين.
    للتصحيح المحلي، فعّل <strong>بيئة التطوير</strong> بضبط المتغير
    <strong>ASPNETCORE_ENVIRONMENT</strong> على <strong>Development</strong>
    ثم أعد تشغيل التطبيق.
</p>
